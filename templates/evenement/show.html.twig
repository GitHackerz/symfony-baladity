<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Evenement</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        table {
            width: 100%;
            margin-bottom: 20px;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            border-bottom: 1px solid #dee2e6;
        }
        th {
            text-align: left;
            background-color: #f0f0f0;
        }
        .btn-container {
            margin-top: 20px;
            display: flex;
            justify-content: center;
        }
        .btn {
            padding: 10px 20px;
            margin-left: 10px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
        }
        .btn-back {
            background-color: #6c757d;
            color: #fff;
        }
        .btn-edit {
            background-color: #ffc107;
            color: #212529;
        }
        .btn-delete {
            background-color: #dc3545;
            color: #fff;
        }
        .title {
            text-align: center;
            text-transform: uppercase;
            margin-bottom:20px;
            

        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">{{evenement.titre}}</h1>
        <table>
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ evenement.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ evenement.titre }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ evenement.description }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ evenement.date }}</td>
                </tr>
                <tr>
                    <th>Lieu</th>
                    <td>{{ evenement.lieu }}</td>
                </tr>
                <tr>
                    <th>NomContact</th>
                    <td>{{ evenement.nomContact }}</td>
                </tr>
                <tr>
                    <th>EmailContact</th>
                    <td>{{ evenement.emailContact }}</td>
                </tr>
                <tr>
                    <th>Statut</th>
                    <td>{{ evenement.statut ? 'Yes' : 'No' }}</td>
                </tr>
            </tbody>
        </table>

        <div class="btn-container">
           <a href="{{ path('app_evenement_index') }}" class="btn btn-back">Back to list</a>
            <a href="{{ path('app_evenement_edit', {'id': evenement.id}) }}" class="btn btn-edit">Edit</a>
            {{ include('evenement/_delete_form.html.twig', {'id': evenement.id}) }}

        </div>
    </div>
</body>
</html>
