<form method="post" action="{{ path('membre_back_delete', {'id': membre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membre.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form> 
