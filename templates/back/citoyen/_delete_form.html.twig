<form method="post" action="{{ path('app_citoyen_delete', {'id': citoyen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ citoyen.id) }}">
    <button class="btn">Delete</button>
</form>
