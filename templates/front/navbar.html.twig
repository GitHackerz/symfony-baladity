<nav class="navbar navbar-expand-lg navbar-dark px-5 py-3 py-lg-0">
    <a href="/" class="navbar-brand p-0">
        <h1 class="m-0">
            <i class="fa fa-user-tie me-2"></i>Baladity</h1>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
        <span class="fa fa-bars"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav ms-auto py-0">
            <a href="/" class="nav-item nav-link {% if app.request.getPathInfo == '/' %}active{% endif %}">Home</a>
            {% if app.user %}
                <a href="/profile"
                   class="nav-item nav-link {% if app.request.getPathInfo == '/profile' %}active{% endif %}">Profile</a>
            {% endif %}
            <a href="/event"
               class="nav-item nav-link {% if app.request.getPathInfo == '/evenement' %}active{% endif %}">Evenement</a>
            <a href="{{ path('project_front_index') }}"
               class="nav-item nav-link {% if app.request.getPathInfo == '/project' %}active{% endif %}">Projet</a>
            <div class="nav-item dropdown">
                <a href="#"
                   class="nav-link dropdown-toggle {% if app.request.getPathInfo == '/demande-association' or app.request.getPathInfo == '/association' %}active{% endif %}"
                   data-bs-toggle="dropdown" aria-expanded="false">Association</a>
                <div class="dropdown-menu m-0 active">
                    <a href="{{ path('demande_association_front_index') }}" class="dropdown-item ">Demander</a>
                    <a href="{{ path('app_association_index') }}" class="dropdown-item">Liste des associations</a>
                </div>
            </div>
            <div class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown"
                   aria-expanded="false">Document</a>
                <div class="dropdown-menu m-0 active">
                    <a href="{{ path('app_demande_doc') }}"
                       class="dropdown-item {% if app.request.getPathInfo == '/document' %}active{% endif %}">Demander</a>
                    <a href="{{ path('app_demande_document_index') }}"
                       class="dropdown-item {% if app.request.getPathInfo == '/document/demandes' %}active{% endif %}">Mes
                        Documents</a>
                </div>
            </div>

            <a href="{{ path('app_reclamation') }}"
               class="nav-item nav-link {% if app.request.getPathInfo == '/reclamation' %}active{% endif %}">Reclamations</a>
        </div>
        <butaton type="button" class="btn text-primary ms-3" data-bs-toggle="modal" data-bs-target="#searchModal">
            <i class="fa fa-search"></i>
        </butaton>
        {% if app.user %}
            {% if app.user.role | lower == 'admin' %}
                <a href="{{ path('app_dashboard') }}" class="btn btn-primary py-2 px-4 ms-3 rounded-3">Dashboard</a>
            {% endif %}
            <a href="{{ path('app_logout') }}" class="btn btn-dark py-2 px-4 ms-3 rounded-3">Logout</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-primary py-2 px-4 ms-3 rounded-3">Login</a>
        {% endif %}
    </div>
</nav>
