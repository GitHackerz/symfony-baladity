<form method="post" action="{{ path('app_demande_document_delete', {'id': demande_document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_document.id) }}">
    <button class="btn">Delete</button>
</form>
